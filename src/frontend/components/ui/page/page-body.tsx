import styled from "styled-components";
import { FlexCol } from "../flex-col";

export const PageBody = styled(FlexCol)`
    position: relative;
    justify-content: center;
    align-items: center;
`;